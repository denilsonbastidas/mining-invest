import Layout from "../components/Layout";

function Profile() {
  return (
    <Layout>
      <section className="flex">
        <section className="text-lg w-full">Profile content</section>
      </section>
    </Layout>
  );
}

export default Profile;
