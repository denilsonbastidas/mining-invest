import Layout from "../components/Layout";

function Quantify() {
  return (
    <Layout>
      <section className="flex">
        <section className="text-lg w-full">Quantify content</section>
      </section>
    </Layout>
  );
}

export default Quantify;
